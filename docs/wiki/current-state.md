# Current State

작성 기준: 2026-07-20

## 최근 반영 완료

- 시험 결과가 0오답이어도 기존 오답 노트 ID를 빈 배열로 덮어쓰지 않음
- 예전 시험 결과의 오답 항목 형태를 로드 시 정규화하고 오답 노트 ID를 복구
- 시험 설정의 최근 결과 패널을 오답 노트와 분리해서 삭제 가능
- 홈의 최근 시험 결과 배너도 오답 노트와 분리해서 삭제 가능
- 시험 결과 화면에서도 최근 결과 기록만 삭제 가능하고 오답 노트는 유지
- 일반 학습 `이전 카드`가 연속 undo 가능
- 학습 카드 전환 시 다음 카드가 너무 빨리 보이던 움찔 현상 완화
- 터치 후 툴팁 포커스 잔류 수정
- 앱 전반의 고비용 시각 효과 축소
- 목록 검색, 가리기, 글자 크기 변경 반응성 개선
- 목록의 일본어/뜻 가리기를 실제 마스킹 바로 변경
- 목록 숨김 토글을 루트 DOM/CSS fast path로 변경해 카드 전체 렌더 없이 즉시 마스킹
- 모바일 목록 숨김 토글을 `pointerdown` fast path, after-paint 저장, touch-action 보강으로 즉시성 개선
- 목록 그리드를 최대 2열로 제한하고 모바일 글자 크기 조정을 DOM/CSS fast path와 after-paint 저장으로 개선
- 목록 카드 즐겨찾기 버튼을 개별 구독과 `Set` 조회로 바꿔 전체 목록 재계산 부담을 줄이고 터치 `pointerdown`에서 바로 반응하도록 개선
- 일반 학습 완료 화면 액션을 홈 이동 1개로 단순화
- 스마트 복습이 `IndexedDB` 일정 저장 구조로 이동
- 비교형 단어장을 학습/시험에서 제거하고 게임·스마트 복습 후보에서도 제외
- 단어장 에디터에서 기본/주제형 스마트 복습 예문을 표 전환과 xlsx로 조회/편집 가능
- 스마트 복습 예문 자동 생성과 어조 메타데이터 제거, 직접 작성 예문만 세션 후보로 사용
- 2026-05-10: 스마트 복습 기능과 관련 UI, 저장소, 에디터 예문 필드를 앱에서 제거
- 2026-05-10: `동사대박살` 단어장의 비동사 항목 품사 오표기를 정정
- 2026-05-11: 백그라운드 복귀 중 페이지 재생성 대비 강화. 게임 세션/결과를 저장 복구하고, 시험/활용형 입력 draft와 학습 카드 전환 pending 상태를 보존
- 2026-06-25: 일반 학습 범위 시작/끝 입력은 편집 중 빈 칸을 허용하고, 빈 값으로 편집 종료 시 이전 수치로 복구
- 2026-06-25: 일반 학습은 세션 시작 시 후보를 새로 셔플해 같은 즐겨찾기 단어장/개수에서도 이전 선택 묶음을 재사용하지 않음
- 2026-07-15: 단어 뜻의 명백한 오역과 모호한 표현을 정리하고, `동사대박살` 안의 동일 표기·읽기 중복 16개를 기존 앞쪽 ID에 통합
- 2026-07-15: 뜻의 괄호 예시를 조사까지 포함하는 자연스러운 한국어 결합 표현으로 정리하고, 슬래시와 괄호 뒤 띄어쓰기 형식을 통일
- 2026-07-15: 목록 모드가 단어장별 스크롤 위치를 저장하고, 같은 단어장에 다시 들어오면 해당 위치를 즉시 복원
- 2026-07-20: 일반 학습에 여러 개의 `반드시 포함할 범위`를 추가하고, 필수 단어 전부와 일반 범위의 무작위 보충 단어로 세션을 구성하도록 변경
- 2026-07-20: 필수 범위가 켜져 있으면 학습 항목 수를 실제 필수 단어 수 이상으로 자동 보정하고, 겹치는 구간은 중복 집계하지 않음
- 2026-07-20: 전체 단어장 1,530개를 표기·읽기·품사 기준으로 대조해 확정 오류 8개 항목을 정정하고 데이터 품질 회귀 테스트를 추가

## 현재 우선순위

- 목록 모드에서 `전체 단어장` 제거
- 진행 중인 학습/시험 파기 팝업을 브라우저 팝업 대신 앱 내부 팝업으로 교체
- 학습 설정 저장 누락 여부 확인
- 공유 메뉴의 불필요한 텍스트와 공간 낭비 축소

## 계속 지켜야 할 주의점

- 미니멀, 아이콘 중심 UI 유지
- 설명 문구 늘리기보다 상태 표현 개선 우선
- blur, filter, height animation, backdrop-filter 재확대 금지에 가깝게 신중히
- 에디터는 모바일 대응보다 데스크톱 표 편집성 우선
- `탭 매치 러시`는 미완이므로 기능 판단 근거에서 제외
